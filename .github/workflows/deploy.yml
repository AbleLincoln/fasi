on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: AbleLincoln/push-to-sftp@0.1
      with:
        host: foodallergyscience.org
        port: 22
        username: b66244294086605
        password: ${{ secrets.ftp_password }}
        # local-dir: ./wp-content/themes/fasi/
        # server-dir: ./wp-content/themes/fasi/
        sourceDir: ./test
        targetDir: ./test